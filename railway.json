{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --prefix proxy"
  },
  "deploy": {
    "startCommand": "node proxy/server.js",
    "healthcheckPath": "/health"
  }
}
