{
  "name": "webhook-bridge-proxy",
  "version": "1.0.0",
  "description": "Secure proxy for Webhook Bridge Tableau extension",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
